name: takalem_sign_talk_demo
description: A new Flutter project.
publish_to: "none"
version: 0.0.1
environment:
  sdk: ">=3.1.5 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  avatar_glow: ^3.0.1
  speech_to_text: ^6.4.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true
  # fonts:
